language: python
python:
  - "3.6"

env:
  - CC="gcc"

install:
  - pip install --upgrade pip
  - pip install poetry
  - cd python && poetry install && cd ..

script:
  - cd python && python setup.py build_ext --inplace nosetests -w tests/test_unit/ && cd ..

