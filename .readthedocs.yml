# Read the Docs configuration file for CleverCSV
#
version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "latest"

sphinx:
  configuration: docs/conf.py

python:
  install:
    - method: pip
      path: .
      extra_requirements:
        - docs
